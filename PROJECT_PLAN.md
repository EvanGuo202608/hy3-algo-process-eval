# hy3-algo-process-eval 项目规划

> **项目展示名：** AlgoTrace-Hy3  
> **GitHub 仓库：** [EvanGuo202608/hy3-algo-process-eval](https://github.com/EvanGuo202608/hy3-algo-process-eval)  
> **项目负责人：** 郭昊杨｜福州三中｜信息学竞赛选手｜NOIP 省一等奖  
> **任务方向：** 犀牛鸟开源·实战任务 2「可验证场景：过程评估与错误定位」—算法竞赛  
> **规划版本：** v1.0  
> **版本日期：** 2026-09-06  
> **当前状态：** 阶段 0 已完成；公开仓库已创建；正在准备阶段 1 的候选题筛选与 8 题 MVP  
> **项目声明：** 本项目是郭昊杨参与犀牛鸟开源实战任务的个人/活动作品，非腾讯官方发布。

---

## 1. 项目概述

### 1.1 一句话定位

AlgoTrace-Hy3 是一个基于 Hy3 的算法竞赛解题与过程评估工具。它将结构化解题说明、C++17 代码、参考解和独立测试数据结合起来，不仅判断程序是否通过测试，还评估解法思路、正确性论证、复杂度分析和边界处理，并定位首个错误步骤、归纳错误类型、输出可复核证据。

### 1.2 项目背景

普通 Online Judge 通常只能根据测试数据判断程序是否通过，能够给出 AC、WA、TLE、MLE、RE 等结果，但很难回答以下问题：

- 解题思路本身是否正确；
- 算法选择是否与数据范围匹配；
- 正确性论证是否成立；
- 时间复杂度和空间复杂度是否分析正确；
- 是否遗漏边界情况；
- 代码错误最早来自哪一步；
- 程序偶然通过测试时，解释是否仍然存在问题；
- 最终答案正确时，过程是否真的能够支撑结论。

本项目希望在传统程序判题基础上加入“过程级评估”，形成一个可运行、可验证、可复现的算法竞赛解题审查系统。

### 1.3 目标用户

本项目面向：

- 信息学竞赛选手；
- 算法课程学生；
- 信息学竞赛教练和教师；
- 需要分析模型算法能力的研究者；
- 希望检查结构化算法解释与实现一致性的开发者。

### 1.4 核心问题

给定：

- 算法竞赛题目；
- 输入输出约束；
- 独立测试数据；
- 模型或选手给出的结构化解法说明；
- C++17 代码；

系统需要输出：

1. 程序能否编译；
2. 程序是否通过规定测试；
3. 解法思路是否正确；
4. 正确性依据是否成立；
5. 时间复杂度和空间复杂度是否正确；
6. 复杂度是否满足题目约束；
7. 边界、溢出、下标和初始化等处理是否充分；
8. 第一个出现错误的步骤；
9. 主要错误类型和次要错误类型；
10. 支撑判定的证据或反例；
11. 错误严重程度和评估置信度；
12. 是否属于“最终答案或程序结果正确，但过程不能支撑结论”。

---

## 2. 项目边界

### 2.1 官方硬要求、项目选择与实施建议

| 维度 | 任务书硬要求 | 本项目当前选择 | 工程实施建议 |
|---|---|---|---|
| 仓库与身份 | 自行创建并维护公开仓库；无需向 Hy3 官方仓库提交 Pull Request；README 需说明项目、运行方式和环境要求；项目需标注为个人/活动作品 | 使用公开仓库 `EvanGuo202608/hy3-algo-process-eval`；展示名暂定 `AlgoTrace-Hy3` | README 首屏明确标注作者、活动作品和“非腾讯官方发布” |
| 模型能力 | 模型能力调用通过 Hy3 完成；不要求训练或微调 | 使用可配置的 OpenAI-compatible Hy3 适配层 | 不写死云端地址；允许用户连接自行部署的 Hy3 兼容端点 |
| 密钥管理 | 不得硬编码或提交 API Key | 使用环境变量和本地 `.env` | 提供 `.env.example`，并通过 `.gitignore` 和秘密扫描防止泄露 |
| 应用输出 | 应用必须输出完整解答过程，不能只给最终答案 | 输出公开、结构化、可检查的解题说明 | 使用 JSON Schema 固定步骤、依赖、论证、复杂度和边界字段 |
| 评测题集 | 每题有明确标准答案和自动校验；按难度分层；说明来源、构造、覆盖和分层依据 | 洛谷用于候选题检索、标签和难度参考；最终测试独立生成 | 优先采用可公开的自编或重新形式化题目，不复制私有测试数据和他人题解 |
| 过程评估 | 判断过程正确性；定位错误开始步骤；错误类型归类；识别结果正确但过程不成立 | 使用 E1—E8 错误分类；只输出一个首错步骤 | 使用确定性执行、规则检查、Hy3 审查和人工复核组成混合评估 |
| 有效性验证 | 至少验证定位准确率和误报率，并保留人工抽检记录 | 同时报告检测召回率、精确/宽松定位准确率、平均步距、FPR 和错误类型 Macro-F1 | gold 标签与评估器预测分开保存，不能把模型预测当作 gold |
| 结果分析 | 输出最终答案准确率、过程正确率、错误类型分布和难度分层结果；指出明显下降区间 | 按难度层和算法类型分别分析 | 小样本只写“观察到的下降”，同时报告样本量和置信区间 |
| 最终产出 | 源码、评估模块、README、环境样例、题集、标准答案、脚本、结果、验证数据、报告和 2 分钟内 demo | 使用标准开源项目目录组织 | 用 CI 运行无密钥测试，并进行一次干净克隆复现 |

### 2.2 真实性边界

本项目遵守以下原则：

- 不虚构 Hy3 调用记录；
- 不虚构人工复核记录；
- 不虚构实验次数；
- 不虚构准确率、误报率或其他指标；
- 不把固定示例结果描述为真实 Hy3 实验结果；
- 不把评估器预测直接当作 gold 标签；
- 不为了提高结果而只保留容易样本；
- 不删除不利结果、超时、解析失败或模型分歧；
- 未完成的功能明确标为 `pending`、`planned` 或“待实现”。

### 2.3 思维过程边界

本项目评估的是模型主动输出的、适合公开验证的结构化解题说明，包括：

- 对题目的理解；
- 关键观察；
- 算法步骤；
- 正确性依据；
- 复杂度分析；
- 边界情况；
- 实现说明；
- C++17 代码。

本项目不要求模型展示或保存不可见的内部思维链，也不将隐藏思维链作为项目数据。

---

## 3. 项目目标

### 3.1 总目标

基于 Hy3 构建一个真实、可运行、可复现的算法竞赛解题与过程评估应用，实现：

- 结构化解答生成；
- C++17 程序编译与测试；
- 解法过程逐步检查；
- 首个错误步骤定位；
- E1—E8 错误分类；
- 答案正确但过程错误的识别；
- 评估器有效性验证；
- 自动生成实验结果和分析报告。

### 3.2 MVP 目标

MVP 计划包含：

- 8 道算法竞赛题；
- A、B、C、D 四个难度层级；
- 每个层级暂定 2 道题；
- 每题至少 4 条带标签轨迹；
- 总轨迹数不少于 32 条；
- 至少包含一个“答案正确但过程不成立”的代表案例；
- 无 Hy3 密钥时，可以通过固定 fixtures 跑通完整工程链路；
- 有 Hy3 端点时，可以切换为真实模型调用。

MVP 必须跑通以下流程：

```text
题目输入
  ↓
Hy3 或 fixture 生成结构化解答
  ↓
JSON Schema 校验
  ↓
C++17 编译与受限执行
  ↓
参考解/检查器判定结果
  ↓
规则检查复杂度与边界
  ↓
Hy3 分步过程审查
  ↓
聚合首错步骤、错误类型、证据和置信度
  ↓
与 gold 标签计算指标
  ↓
导出 JSONL、CSV、图表和典型案例
```

### 3.3 正式版目标

正式版计划包含：

- 12 道题；
- 每题约 6 条轨迹；
- 总轨迹数约 72 条；
- 四象限样本覆盖；
- 真实 Hy3 运行记录；
- 完整人工抽检记录；
- 最终结果报告；
- 2 分钟以内的 demo 视频或 GIF；
- GitHub Actions 基础 CI；
- 干净克隆复现验证。

正式版难度分布暂定：

| 层级 | 目标题数 | 主要方向 | 重点错误模式 |
|---|---:|---|---|
| A 基础 | 2 | 模拟、枚举、字符串、数组 | 题意误读、最小规模、下标、输入输出格式 |
| B 普及 | 3 | 贪心、排序、二分、基础数据结构 | 贪心缺少证明、单调性错误、二分边界错误 |
| C 普及+/提高- | 4 | 动态规划、图论、并查集、树状数组或线段树 | 状态遗漏、转移错误、复杂度不达标、整数溢出 |
| D 提高 | 3 | 综合建模、树图算法、较复杂数据结构 | 隐含条件、错误不变量、仅通过弱测试或样例 |

以上题量是项目实施目标，不是任务书规定的最低题数。如果开发时间不足，优先保证 8 题 MVP 的测试、标注和验证质量。

---

## 4. 题集设计

### 4.1 洛谷的使用方式

洛谷用于：

- 检索候选题；
- 参考当前难度；
- 查看算法标签；
- 记录题号、题目链接和来源；
- 帮助形成从基础到提高的难度覆盖。

洛谷不直接等同于本项目最终评测集。

本项目不会：

- 抓取洛谷私有测试数据；
- 绕过洛谷限制下载测试数据；
- 批量向洛谷提交程序；
- 复制他人题解；
- 把洛谷难度当作完全客观的唯一标准；
- 在没有确认许可的情况下完整转载题面。

### 4.2 候选题筛选条件

一道题进入 MVP 前必须通过以下检查：

1. **来源可说明**：能够记录题号、链接、原始来源和访问日期；
2. **许可可说明**：能够说明仓库中保存什么、不保存什么；
3. **离线可复现**：不依赖在线评测才能完成基础验证；
4. **测试可独立生成**：可以编写生成器、参考解和检查器；
5. **错误模式有价值**：能够覆盖至少一种值得研究的过程错误；
6. **算法覆盖互补**：不会与已有题目完全重复；
7. **难度适合分层**：能够明确解释所在层级；
8. **规模适合实验**：能在合理资源限制下重复运行。

### 4.3 候选检索起点

候选检索可从以下洛谷题目开始，但这些题目目前只是候选，不代表已经正式入选：

- P1909；
- P1047；
- P1094；
- P1873；
- P1090；
- P2678；
- P3373；
- P5658。

正式选题前必须逐题核查：

- 当前页面是否存在；
- 当前难度；
- 来源；
- 算法标签；
- 题面能否转载；
- 是否仅保存链接与自己的形式化规格；
- 是否能独立生成测试；
- 是否能够构造有价值的错误解法。

### 4.4 自有难度分层依据

最终难度不只依赖洛谷颜色，而是综合记录：

- 洛谷当前难度；
- 算法知识点数量；
- 思维复杂度；
- 实现复杂度；
- 正确性证明难度；
- 边界数量；
- 数据范围；
- 错误模式复杂度；
- 生成测试与构造反例的难度。

每道题的 `metadata.yaml` 至少记录：

```yaml
problem_id: ""
title: ""
source_platform: ""
source_url: ""
source_note: ""
license_or_usage_note: ""
accessed_at: "2026-09-06"

difficulty:
  external_label: ""
  internal_tier: "A|B|C|D"
  reasoning: ""

algorithms: []
implementation_topics: []
expected_error_types: []

limits:
  time_ms: 2000
  memory_mb: 256

reproducibility:
  generator_seed: 20260906
  reference_solution: true
  checker: true
  offline_tests: true
```

### 4.5 单题目录

每道题至少包含：

```text
benchmarks/problems/<problem_id>/
├── README.md
├── metadata.yaml
├── statement_or_spec.md
├── reference_solution.cpp
├── generator.py
├── checker.py
├── rubric.yaml
└── tests/
    ├── sample/
    ├── boundary/
    ├── adversarial/
    └── random/
```

### 4.6 测试覆盖

根据题型选择以下测试：

- 官方或自编样例；
- 最小规模；
- 最大规模；
- 空集合或单元素；
- 重复值；
- 全部相等；
- 严格递增；
- 严格递减；
- 极端图或树结构；
- 不连通情况；
- 整数溢出；
- 二分边界；
- DP 初始化；
- 状态转移顺序；
- 复杂度卡点；
- 随机性质测试；
- 能区分正确解和典型错误解的对抗测试。

随机生成器必须使用固定种子，并支持一键重新生成。

---

## 5. 结构化解答协议

### 5.1 设计目的

为了准确定位错误，解答不能只是自由文本。生成器和评估器共享统一的 JSON Schema，使每个步骤都具有：

- 唯一编号；
- 步骤类型；
- 可核验陈述；
- 简短依据；
- 依赖步骤。

### 5.2 基础结构

```json
{
  "problem_id": "example_problem",
  "problem_understanding": "简洁重述输入、输出、约束和目标",
  "steps": [
    {
      "step_id": 1,
      "kind": "observation",
      "claim": "可独立核验的陈述",
      "justification": "公开、简洁的依据",
      "depends_on": []
    },
    {
      "step_id": 2,
      "kind": "algorithm",
      "claim": "算法步骤",
      "justification": "该步骤成立的原因",
      "depends_on": [1]
    }
  ],
  "algorithm": "完整算法摘要",
  "correctness_argument": "可公开检查的正确性依据",
  "time_complexity": "O(n log n)",
  "space_complexity": "O(n)",
  "boundary_cases": [
    "n = 1",
    "所有元素相等",
    "最大数据范围"
  ],
  "code_language": "cpp17",
  "code": "#include <bits/stdc++.h>...",
  "final_output_or_verdict": "程序预期行为或最终结论"
}
```

### 5.3 步骤类型

`kind` 允许以下类型：

- `observation`：关键观察；
- `algorithm`：算法步骤；
- `proof`：正确性依据；
- `complexity`：复杂度分析；
- `boundary`：边界与特殊情况；
- `implementation`：实现细节。

### 5.4 输出约束

- `step_id` 必须唯一并按顺序排列；
- `depends_on` 只能引用已经出现的步骤；
- 每条 `claim` 应是可核验陈述；
- 不允许只写“显然”“容易知道”而没有必要依据；
- 复杂度必须与实际实现进行对照；
- 代码必须注明语言版本；
- Schema 无法解析时，记录解析失败，不能静默丢弃样本。

---

## 6. 错误分类体系

### E1：题意或约束误读

包括：

- 输入输出理解错误；
- 目标理解错误；
- 数据范围理解错误；
- 忽略特殊条件；
- 混淆严格与非严格关系；
- 忽略题目保证。

### E2：建模或算法选择错误

包括：

- 问题模型不正确；
- 算法不保证正确；
- 使用不适用的贪心策略；
- 错误判断单调性；
- 使用错误的数据结构或状态表示。

### E3：正确性论证错误

包括：

- 不变量错误；
- 归纳假设错误；
- 贪心交换论证错误；
- 充分条件与必要条件混淆；
- 循环论证；
- 关键步骤缺失；
- 结论超出前提。

### E4：复杂度错误

包括：

- 声称复杂度与代码不一致；
- 时间复杂度超过约束；
- 空间复杂度超过约束；
- 忽略数据结构操作复杂度；
- 忽略重复遍历或嵌套循环。

### E5：边界或数值错误

包括：

- 数组越界；
- 空集合；
- 单元素；
- 重复值；
- 初始化错误；
- 整数溢出；
- 模运算错误；
- 浮点精度错误；
- 二分端点错误。

### E6：实现逻辑错误

包括：

- 代码偏离正确思路；
- 状态更新次序错误；
- 数据结构使用错误；
- 遗漏分支；
- 错误变量；
- 读写格式错误。

### E7：测试过拟合或偶然正确

包括：

- 只对样例成立；
- 只对弱测试成立；
- 猜中答案；
- 数值巧合；
- 错误实现恰好通过当前测试；
- 针对固定数据硬编码。

### E8：幻觉、自相矛盾或证据不足

包括：

- 使用不存在的定理或性质；
- 步骤之间互相矛盾；
- 引用不存在的约束；
- 结论没有可验证支撑；
- 解释与代码明显不一致。

### 6.1 首错约束

评估结果可以包含多个错误类型：

```json
{
  "primary_error_type": "E3",
  "secondary_error_types": ["E4", "E5"]
}
```

但是：

```json
{
  "first_error_step": 3
}
```

只能有一个首错步骤。

如果没有发现错误：

```json
{
  "first_error_step": null,
  "primary_error_type": null,
  "secondary_error_types": []
}
```

所有错误分类都必须包含可复核证据，不能只输出一个类型编号。

---

## 7. 评估器架构

### 7.1 总体流程

```text
结构化解答
  │
  ├── Schema 校验
  │
  ├── 代码编译与受限执行
  │
  ├── 参考解和检查器判定
  │
  ├── 复杂度与边界规则检查
  │
  ├── Hy3 分步审查
  │
  ├── 低置信度/冲突复核
  │
  └── 聚合评估结果
         ├── final_correct
         ├── process_correct
         ├── first_error_step
         ├── error_types
         ├── evidence
         ├── severity
         └── confidence
```

### 7.2 确定性层

确定性层负责：

- JSON Schema 校验；
- 必填字段检查；
- 步骤编号检查；
- 依赖关系检查；
- C++17 编译；
- 测试执行；
- 超时与内存限制；
- 标准输出比较；
- Special Judge 或自定义检查器；
- 性质测试；
- 随机测试；
- 反例最小化。

### 7.3 规则层

规则层负责检查：

- 复杂度声明是否缺失；
- 声明复杂度是否与明显代码结构矛盾；
- 数据范围是否可能导致超时；
- 是否存在明显整数溢出风险；
- 是否覆盖必要边界；
- 是否存在常见下标错误；
- 是否可能发生未初始化使用；
- 是否存在格式或 Schema 问题。

规则层无法证明的事项必须输出：

```text
unknown
```

不能为了得到完整结论而强行判断。

### 7.4 Hy3 审查层

Hy3 审查层负责：

- 逐步检查 `claim`；
- 检查 `depends_on` 是否足以支撑当前步骤；
- 检查关键不变量；
- 检查正确性论证；
- 检查复杂度说明；
- 检查边界分析；
- 检查说明与代码是否一致；
- 给出可能的首错步骤；
- 给出错误类型、证据、严重程度和置信度。

Hy3 审查采用：

- 固定系统提示词；
- 固定 rubric；
- 固定 JSON Schema；
- 可配置模型名称；
- 可配置低随机性参数；
- 可配置重复次数；
- 保存脱敏后的原始响应；
- 保存解析失败和重试日志。

### 7.5 复核层

以下情况触发复核：

- 规则层与 Hy3 结论冲突；
- 首错置信度低；
- 程序通过全部测试但过程被判错误；
- 高难题；
- 同一样本重复审查结论不一致；
- 评估器发现可能存在多个首错位置。

复核方式可以是：

- 第二次独立 Hy3 审查；
- 使用不同提示顺序复核；
- 人工复核；
- 教练或另一名选手复核。

### 7.6 安全层

候选 C++ 代码视为不可信输入。

执行器应尽可能：

- 禁用网络；
- 限制运行时间；
- 限制内存；
- 限制输出大小；
- 限制文件访问；
- 限制进程数量；
- 使用临时目录；
- 运行后清理临时文件；
- 保存超时、异常退出和信号信息。

如果当前平台不能可靠隔离，README 必须明确说明：

- 不建议执行来源不明的代码；
- 默认示例只执行仓库内固定 fixture；
- 本地执行陌生代码可能存在安全风险。

---

## 8. Hy3 适配设计

### 8.1 配置方式

Hy3 适配器从环境变量读取配置：

```text
HY3_BASE_URL
HY3_API_KEY
HY3_MODEL
HY3_TIMEOUT
HY3_TEMPERATURE
HY3_MAX_RETRIES
```

`.env.example` 只保存变量名称和示例，不保存真实密钥：

```dotenv
HY3_BASE_URL=http://127.0.0.1:8000/v1
HY3_API_KEY=replace-with-local-secret
HY3_MODEL=hy3
HY3_TIMEOUT=120
HY3_TEMPERATURE=0.1
HY3_MAX_RETRIES=2
```

真实 `.env` 必须加入 `.gitignore`。

### 8.2 离线模式

没有 Hy3 端点时：

- 使用固定请求与固定响应 fixtures；
- 测试 JSON Schema；
- 测试错误处理；
- 测试解析失败；
- 测试评估聚合；
- 测试报告生成；
- 明确标记结果为 `fixture` 或 `mock`；
- 不称为真实 Hy3 实验。

### 8.3 在线模式

有 Hy3 端点时：

- 记录模型名称；
- 记录参数；
- 记录提示词版本；
- 记录运行时间；
- 记录数据集版本；
- 记录随机种子；
- 记录解析失败和重试；
- 保存脱敏响应；
- 不保存 API Key；
- 不在公开日志中保存个人敏感信息。

---

## 9. 样本与标注设计

### 9.1 四象限样本

正式数据集覆盖以下四类：

| 象限 | 最终结果 | 解题过程 | 目的 |
|---|---|---|---|
| Q1 | 正确 | 正确 | 测试正常通过并控制误报 |
| Q2 | 正确 | 错误或证据不足 | 检验“答案正确但过程不成立”的识别能力 |
| Q3 | 错误 | 前段合理，后续实现或边界错误 | 检验首错定位 |
| Q4 | 错误 | 错误 | 检验错误检测和分类能力 |

### 9.2 样本来源

样本可以来自：

- 人工编写的正确解；
- Hy3 真实输出；
- 固定模型 fixture；
- 单一错误注入；
- 多错误注入；
- 正确思路加错误实现；
- 错误思路加偶然正确实现；
- 错误解释加正确代码；
- 正确解释加错误代码。

### 9.3 错误注入记录

每次错误注入必须记录：

```json
{
  "sample_id": "problem01_trace03",
  "source_trace": "problem01_trace01",
  "injection_type": "E5",
  "modified_step": 4,
  "expected_first_error_step": 4,
  "description": "将二分右边界由 n-1 改为 n",
  "created_by": "human",
  "review_status": "pending"
}
```

### 9.4 Gold 标签

每条轨迹的 gold 至少包含：

```json
{
  "sample_id": "problem01_trace03",
  "final_correct": false,
  "process_correct": false,
  "first_error_step": 4,
  "error_types": ["E5", "E6"],
  "evidence": "右边界可能访问数组下标 n",
  "annotator": "郭昊杨",
  "review_status": "reviewed"
}
```

评估器自己的预测必须单独保存，例如：

```json
{
  "sample_id": "problem01_trace03",
  "predicted_process_correct": false,
  "predicted_first_error_step": 4,
  "predicted_error_types": ["E5"],
  "confidence": 0.91
}
```

不得用预测结果覆盖 gold。

### 9.5 人工复核

至少人工复核：

- 所有 gold 过程错误样本；
- 所有被预测为“答案正确但过程错误”的样本；
- 所有低置信度样本；
- 所有规则层和 Hy3 冲突的样本；
- 所有高难题的代表样本。

如果只有一名标注者，报告中明确说明：

- gold 由单人标注；
- 没有计算标注者一致率；
- 这是实验局限。

如果能邀请教练或另一名选手复核，则记录：

- 两名标注者的独立结果；
- 一致样本数；
- 分歧样本数；
- 分歧原因；
- 最终裁决；
- 一致率或 Cohen's Kappa 等指标。

---

## 10. 指标定义

所有指标在正式实验前固定。

### 10.1 最终答案准确率

```text
Final Answer Accuracy
= 通过全部规定测试的解答数量 / 解答总数
```

### 10.2 过程正确率

```text
Process Correct Rate
= 经 gold 或人工确认过程成立的解答数量 / 解答总数
```

报告时应区分：

- gold 过程正确率；
- 评估器预测的过程正确率。

不能把二者混写。

### 10.3 错误检测召回率

```text
Error Detection Recall
= 正确识别为“过程有问题”的错误轨迹数量
  / gold 过程错误轨迹总数
```

### 10.4 精确定位准确率

```text
Exact Localization Accuracy
= 预测首错步骤与 gold 首错步骤完全相同的错误轨迹数量
  / gold 过程错误轨迹总数
```

### 10.5 宽松定位准确率

```text
Relaxed Localization Accuracy
= 预测首错步骤位于 gold 步骤 ±1 范围内的错误轨迹数量
  / gold 过程错误轨迹总数
```

同时报告：

```text
Mean Absolute Step Distance
= 预测首错步骤与 gold 首错步骤的平均绝对距离
```

### 10.6 误报率

```text
False Positive Rate
= 被误判为过程错误的 gold 过程正确轨迹数量
  / gold 过程正确轨迹总数
```

### 10.7 答案正确样本专项抽检

任务书特别要求对“答案正确但被评估器判为过程有问题”的样本进行人工抽检。

因此需要单独报告：

- 答案正确样本总数；
- 被评估器标记为过程有问题的数量；
- 人工确认其中确有过程问题的数量；
- 真实问题比例；
- 人工确认属于误报的数量；
- 误报比例。

不能把人工确认的真实过程问题误称为误报。

### 10.8 错误类型 Macro-F1

按照 E1—E8 分别计算：

- Precision；
- Recall；
- F1；
- Macro-F1。

某一类别没有足够样本时显示：

```text
N/A
```

不能用 `0` 掩盖样本缺失。

### 10.9 难度分层分析

按 A、B、C、D 四个层级报告：

- 样本数量；
- Final Answer Accuracy；
- Process Correct Rate；
- Error Detection Recall；
- Exact Localization Accuracy；
- Relaxed Localization Accuracy；
- False Positive Rate；
- 错误类型分布。

### 10.10 能力下降区间

正式实验前预先声明临界点判断方法，例如：

```text
把相邻难度层之间“过程正确率下降最多”的位置，
作为观察到的主要下降区间。
```

同时必须报告：

- 各层样本量；
- 原始比例；
- 下降幅度；
- bootstrap 置信区间；
- 是否具有足够样本支持结论。

样本较少时，只能写：

```text
观察到模型在 C 到 D 层出现较明显下降。
```

不能写成：

```text
已经证明模型的能力临界点是 D 层。
```

---

## 11. 仓库结构规划

```text
hy3-algo-process-eval/
├── README.md
├── PROJECT_PLAN.md
├── LICENSE
├── .gitignore
├── .env.example
├── pyproject.toml
│
├── docs/
│   └── planning/
│       ├── README.md
│       ├── requirement_traceability.md
│       ├── 郭昊杨_AlgoTrace-Hy3_阶段0需求追踪与项目规划_v1.docx
│       └── 郭昊杨_AlgoTrace-Hy3_阶段0需求追踪与项目规划_v1.pdf
│
├── src/
│   └── algotrace_hy3/
│       ├── __init__.py
│       ├── cli.py
│       ├── app.py
│       ├── schemas.py
│       ├── config.py
│       ├── hy3_client.py
│       │
│       ├── generation/
│       │   ├── __init__.py
│       │   ├── prompt.py
│       │   └── generator.py
│       │
│       ├── evaluator/
│       │   ├── __init__.py
│       │   ├── rules.py
│       │   ├── sandbox.py
│       │   ├── llm_judge.py
│       │   ├── aggregate.py
│       │   └── counterexample.py
│       │
│       └── reporting/
│           ├── __init__.py
│           ├── metrics.py
│           ├── tables.py
│           └── charts.py
│
├── benchmarks/
│   ├── problem_catalog.yaml
│   └── problems/
│       └── <problem_id>/
│           ├── README.md
│           ├── metadata.yaml
│           ├── statement_or_spec.md
│           ├── reference_solution.cpp
│           ├── generator.py
│           ├── checker.py
│           ├── rubric.yaml
│           └── tests/
│
├── annotations/
│   ├── guideline.md
│   ├── gold.jsonl
│   ├── injection_log.jsonl
│   ├── manual_audit.csv
│   └── disagreements.csv
│
├── experiments/
│   └── configs/
│       ├── offline_fixture.yaml
│       └── main.yaml
│
├── results/
│   ├── README.md
│   ├── raw/
│   ├── summary.csv
│   ├── metrics.json
│   └── figures/
│
├── reports/
│   ├── methodology.md
│   ├── analysis.md
│   └── final_report.pdf
│
├── demo/
│   ├── README.md
│   ├── script.md
│   └── recording_checklist.md
│
├── scripts/
│   ├── validate_problem_catalog.py
│   ├── build_dataset.py
│   ├── reproduce_problem.py
│   ├── run_eval.py
│   ├── compute_metrics.py
│   ├── validate_results.py
│   ├── build_report.py
│   ├── scan_secrets.py
│   └── reproduce_all.py
│
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── evaluator/
│   ├── golden/
│   └── sandbox/
│
└── .github/
    └── workflows/
        └── ci.yml
```

---

## 12. 阶段路线图

总路线按阶段 0—7 编号，共 8 个阶段。阶段 0 是需求基线，阶段 1—7 是 7 个实施与交付阶段。

### 阶段 0：需求追踪

#### 目标

区分任务书硬要求、个人选择和工程建议，建立可追溯的项目基线。

#### 交付物

- 原始任务书核对；
- 项目一句话定位；
- 官方要求/个人选择/实施建议三栏边界；
- MVP 与正式版范围；
- 需求追踪矩阵；
- 阶段路线图；
- 风险登记；
- 阻塞项；
- Word 和 PDF 正式规划文档。

#### 验收

```bash
test -f PROJECT_PLAN.md
test -f docs/planning/requirement_traceability.md
```

#### 状态

```text
已完成
```

---

### 阶段 1：选题与方案

#### 目标

建立候选题矩阵，从中冻结 8 题 MVP。

#### 交付物

- 16—20 道候选题矩阵；
- 每题题号、链接、来源和访问日期；
- 当前洛谷难度和算法标签；
- 许可/引用检查；
- 自有难度分层规则；
- 算法覆盖矩阵；
- 错误模式覆盖矩阵；
- 8 题 MVP 名单；
- E1—E8 标注规范。

#### 验收命令

```bash
python scripts/validate_problem_catalog.py
```

#### 主要风险

- 题面版权不清；
- 题目类型过于集中；
- 难度分层依赖主观颜色；
- 题目无法独立生成测试；
- 错误模式重复。

#### 当前状态

**阶段状态：已完成（2026-09-07）**

已完成 20 题候选矩阵、8 题 MVP 冻结、12 题正式版扩展规划、E1-E8 错误覆盖和标注规范。阶段一设计详见 [stage1_problem_selection.md](docs/planning/stage1_problem_selection.md)。

---

### 阶段 2：工程骨架

#### 目标

建立可安装、可测试、可调用的本地工程。

#### 交付物

- Python 包；
- 命令行接口；
- 配置模块；
- JSON Schema；
- Hy3 适配器；
- `.env.example`；
- `.gitignore`；
- 单元测试；
- GitHub Actions；
- 第一题纵向切片。

#### 验收命令

```bash
python -m pytest -q
python -m algotrace_hy3 --help
```

#### 主要风险

- 过早开发复杂界面；
- 工程结构过大；
- 无密钥时无法测试；
- 配置和代码耦合。

#### 控制措施

先完成一题、一个 fixture、一个评估结果的最小纵向切片。

#### 当前状态

```text
待开始
```

---

### 阶段 3：题集与安全执行

#### 目标

完成可复现题集、参考解、测试生成和安全代码执行。

#### 交付物

- 参考解；
- 固定种子生成器；
- 检查器；
- 样例测试；
- 边界测试；
- 对抗测试；
- 性质测试；
- 受限 C++17 编译执行器；
- 超时与资源限制；
- 反例最小化。

#### 验收命令

```bash
python scripts/reproduce_problem.py --all --seed 20260906
```

#### 主要风险

- 不可信代码危害主机；
- 不同平台行为不一致；
- 测试过弱；
- 生成器和参考解共享同一错误。

#### 控制措施

- 参考解和生成器独立检查；
- 设置 CPU、内存、时间和输出限制；
- 禁止网络；
- 保留失败日志；
- 隔离不足时默认不执行陌生代码。

#### 当前状态

```text
待开始
```

---

### 阶段 4：过程评估

#### 目标

实现过程正确性判断、首错定位、错误分类和答案正确/过程错误识别。

#### 交付物

- Schema 检查；
- 规则评估器；
- Hy3 分步评估器；
- 聚合器；
- E1—E8 分类；
- 首错定位；
- 证据与置信度；
- `unknown` 状态；
- 冲突复核；
- golden tests。

#### 验收命令

```bash
pytest tests/evaluator -q
pytest -k correct_answer_wrong_process -q
```

#### 主要风险

- Hy3 评估波动；
- 评估器把“简略”误判为“错误”；
- 无证据强行判断；
- 解释与代码的错误来源混淆。

#### 控制措施

- 固定 rubric；
- 固定模型和参数；
- 保存脱敏原始响应；
- 支持 `unknown`；
- 对冲突和低置信度样本复核。

#### 当前状态

```text
待开始
```

---

### 阶段 5：实验验证

#### 目标

冻结数据和配置，运行真实实验，计算任务书要求的指标。

#### 交付物

- 冻结题集版本；
- 冻结提示词版本；
- 冻结实验配置；
- 原始 JSONL；
- 评估预测；
- gold 标签；
- 人工抽检；
- 定位准确率；
- 误报率；
- 错误类型指标；
- 按难度和算法类型分层结果；
- 失败和重试日志。

#### 验收命令

```bash
python scripts/run_eval.py --config experiments/configs/main.yaml
python scripts/compute_metrics.py
python scripts/validate_results.py
```

#### 主要风险

- 缺少 Hy3 端点；
- 数据标签泄漏；
- 样本数量过少；
- 人工标注不一致；
- 运行中途失败。

#### 控制措施

- 无端点时只报告 fixture 状态；
- gold 与预测分开保存；
- 冻结题集后再运行；
- 保留失败样本；
- 真实结果保存时间戳和配置快照。

#### 当前状态

```text
待开始
```

---

### 阶段 6：报告与演示

#### 目标

形成完整分析报告和不超过 2 分钟的演示材料。

#### 交付物

- 最终答案准确率表；
- 过程正确率表；
- 定位准确率表；
- 误报率表；
- 错误类型分布；
- 难度分层图；
- 算法类型分层图；
- 典型案例；
- 模型能力边界；
- 项目局限；
- 完整分析报告；
- 不超过 2 分钟的 demo 视频或 GIF。

#### Demo 内容

```text
选题
  → Hy3 生成结构化解答
  → C++17 编译与测试
  → 逐步过程评估
  → 定位首错步骤
  → 输出错误类型和证据
  → 展示结果报告
```

#### 验收命令

```bash
python scripts/build_report.py
```

并人工确认：

- demo 时长不超过 120 秒；
- 报告数字可追溯到结果文件；
- 图表与 CSV 一致；
- 未完成内容没有被写成完成。

#### 主要风险

- 只展示成功案例；
- 选择性报告；
- 夸大模型能力临界点；
- 图表与原始结果不一致。

#### 当前状态

```text
待开始
```

---

### 阶段 7：GitHub 最终交付

#### 目标

将经过验证的项目作为公开开源仓库正式交付。

#### 交付物

- 完整公开仓库；
- README；
- 许可证；
- 环境配置样例；
- 源码；
- 题集；
- 测试；
- 评估脚本；
- 完整结果；
- 人工抽检；
- 报告；
- demo；
- CI；
- 干净克隆复现记录。

#### 验收命令

```bash
git status --short
git remote -v
python scripts/scan_secrets.py
python -m pytest -q
python scripts/reproduce_all.py --offline
```

还需要在新的临时目录中完成一次：

```text
克隆公开仓库
→ 安装依赖
→ 运行无密钥示例
→ 运行单元测试
→ 生成示例结果
```

#### 主要风险

- 提交 API Key；
- 提交私人邮箱或绝对路径；
- README 与实际命令不一致；
- CI 依赖真实密钥；
- 本机可以运行，但干净环境无法复现。

#### 当前状态

```text
仓库已创建，最终交付待完成
```

---

## 13. 当前需求追踪摘要

| ID | 要求 | 计划文件或模块 | 当前状态 |
|---|---|---|---|
| SUB-01 | 创建公开仓库并提交链接 | GitHub 仓库 | 已完成 |
| SUB-02 | README 说明项目、运行方式和环境 | `README.md` | 正在提交 |
| SUB-03 | API Key 不进入仓库 | `.env.example`、`.gitignore`、秘密扫描 | 待实现 |
| SUB-04 | 标明个人/活动作品，非腾讯官方 | README、报告、文档首页 | 规划中已完成 |
| SUB-05 | 模型能力通过 Hy3 完成 | `hy3_client.py`、实验配置 | 待实现 |
| APP-01 | 应用输出完整解答过程 | CLI、Schema、生成模块 | 待实现 |
| SET-01 | 每题有标准答案和自动校验 | `benchmarks/problems/` | 待实现 |
| SET-02 | 题目按难度分层并说明依据 | `problem_catalog.yaml` | 待实现 |
| EVAL-01 | 判断过程是否正确 | 规则和 Hy3 评估器 | 待实现 |
| EVAL-02 | 定位首个错误步骤 | 聚合器、定位测试 | 待实现 |
| EVAL-03 | 建立错误分类体系 | E1—E8、标注规范 | 设计已完成，代码待实现 |
| EVAL-04 | 识别答案正确但过程不成立 | 四象限样本、golden tests | 待实现 |
| VAL-01 | 验证定位准确率 | gold、指标脚本 | 待实现 |
| VAL-02 | 验证误报率并保留人工抽检 | `manual_audit.csv` | 待实现 |
| RES-01 | 输出完整指标和错误分布 | `results/` | 待实现 |
| RES-02 | 按难度分析下降区间 | `reports/analysis.md` | 待实现 |
| OUT-01 | 源码、README、环境样例和运行说明 | 仓库根目录、`src/` | 待实现 |
| OUT-02 | 题集、标准答案和评估脚本 | `benchmarks/`、`scripts/` | 待实现 |
| OUT-03 | 完整结果、验证数据和抽检记录 | `results/`、`annotations/` | 待实现 |
| OUT-04 | 完整分析报告 | `reports/` | 待实现 |
| OUT-05 | 2 分钟内 demo | `demo/` | 待实现 |

---

## 14. 风险登记

| 风险 | 概率 | 影响 | 触发信号 | 应对 |
|---|---|---|---|---|
| 把解释简略误判为过程错误 | 中 | 高 | 正确轨迹误报率偏高 | rubric 区分“简略”“证据不足”“错误”和 `unknown` |
| 测试通过但算法逻辑有缺陷 | 高 | 高 | 典型错误解也通过全部测试 | 增加性质测试、对抗测试和反例最小化 |
| 复杂度声明与实现不一致 | 中 | 中 | 文本声称 O(n log n)，代码出现明显嵌套扫描 | 规则提示风险，结合规模测试和 Hy3 复核 |
| 不可信代码危害主机 | 中 | 高 | 代码访问网络、文件或创建大量进程 | 禁网、限时、限存、限进程、临时目录和安全警告 |
| 题集规模挤压标注质量 | 高 | 高 | 为达到 12 题而跳过 gold 或测试 | 先冻结并完成 8 题 MVP，再扩展 |
| Hy3 评审波动导致结果不可复现 | 中 | 高 | 同一样本多次判断不一致 | 固定模型、参数、提示词和数据版本，报告重复波动 |
| Gold 标签受到预测污染 | 中 | 高 | 标注者先看评估器答案再标注 | gold 独立标注，预测单独保存 |
| 公开仓库泄露密钥 | 低 | 极高 | `.env`、访问令牌或日志进入暂存区 | `.gitignore`、秘密扫描、人工检查和干净克隆 |
| 题面或数据版权不清 | 中 | 高 | 无法说明仓库材料来源与许可 | 未确认许可时只保存链接、元数据和自己的形式化规格 |
| 小样本导致临界点结论不可靠 | 高 | 中 | 某难度层样本过少 | 报告样本量和置信区间，只称“观察到的下降” |

---

## 15. 当前阻塞项

以下事项不会阻塞阶段 1—4 的本地开发，但会阻塞对应的最终里程碑。

### 15.1 真实 Hy3 实验资源

如果没有可用的 Hy3 端点、模型标识和本地环境变量，只能完成离线 fixture 和工程验证，不能声称已经完成真实 Hy3 实验。

### 15.2 最终许可证

仓库已经公开，但最终许可证仍需确定。

暂定候选：

```text
Apache License 2.0
```

正式添加许可证前需要再次确认。

### 15.3 题目许可和引用策略

每道题是否可以完整收录、是否只能保留链接、哪些内容由自己重新形式化，需要在阶段 1 逐题检查。

### 15.4 第二标注者

如果无法邀请教练或另一名选手复核，可以完成单人 gold，但不能报告标注者一致率，需要在最终报告中明确说明局限。

### 15.5 活动截止日期与提交入口

当前任务书没有给出明确的截止日期和最终提交表单规则。它不影响本地开发，但会影响最后排期。

---

## 16. 近期工作清单

### 当前：阶段 0

- [x] 阅读并核对原始任务书；
- [x] 确认任务 2 算法竞赛方向；
- [x] 建立官方要求、个人选择和实施建议三栏边界；
- [x] 完成 MVP 与正式版范围；
- [x] 建立需求追踪矩阵；
- [x] 完成阶段路线图；
- [x] 完成风险和阻塞项分析；
- [x] 创建公开 GitHub 仓库；
- [x] 提交仓库链接；
- [x] 准备 Markdown、Word 和 PDF 规划文件；
- [ ] 将规划文件提交到 GitHub；
- [ ] 完成仓库首页 README。

### 下一步：阶段 1

- [ ] 创建候选题信息表；
- [ ] 收集 16—20 道候选题；
- [ ] 记录题号、链接、来源和访问日期；
- [ ] 记录当前难度和算法标签；
- [ ] 检查许可与引用方式；
- [ ] 分析能否独立生成测试；
- [ ] 为每题设计预期错误模式；
- [ ] 检查算法覆盖是否互补；
- [ ] 筛选 8 题 MVP；
- [ ] 固化第一版题目目录和 metadata 格式；
- [ ] 固化 E1—E8 标注规范。

### 阶段 2 准备项

- [ ] 确认 Python 版本；
- [ ] 确认本地 C++17 编译器；
- [ ] 创建 `pyproject.toml`；
- [ ] 创建包目录；
- [ ] 创建 `.gitignore`；
- [ ] 创建 `.env.example`；
- [ ] 建立第一题纵向切片；
- [ ] 添加无密钥测试；
- [ ] 添加基础 CI。

---

## 17. 完成定义

项目满足以下条件后才能称为完成：

### 工程可运行

- [ ] 新环境可以按 README 安装；
- [ ] 无 Hy3 密钥时固定示例可运行；
- [ ] 有 Hy3 端点时可以切换为真实调用；
- [ ] C++17 编译、测试和评估链路可运行；
- [ ] CI 基础测试通过。

### 题集可复现

- [ ] 至少完成 8 题 MVP；
- [ ] 正式目标达到 12 题；
- [ ] 每题有来源和分层说明；
- [ ] 每题有参考解；
- [ ] 每题有自动校验；
- [ ] 每题有固定种子生成器或固定测试；
- [ ] 每题有针对错误模式的测试。

### 过程评估完整

- [ ] 能判断过程正确性；
- [ ] 能定位首错步骤；
- [ ] 能归类 E1—E8 错误；
- [ ] 能识别答案正确但过程不成立；
- [ ] 能输出证据；
- [ ] 能输出严重程度和置信度；
- [ ] 无法证明时能够输出 `unknown`。

### 验证真实

- [ ] Gold 与预测分开保存；
- [ ] 运行真实实验；
- [ ] 保存原始输出和配置；
- [ ] 计算定位准确率；
- [ ] 计算误报率；
- [ ] 完成人工抽检；
- [ ] 保留失败、超时、解析失败和评估分歧；
- [ ] 未虚构实验和结果。

### 报告完整

- [ ] 输出最终答案准确率；
- [ ] 输出过程正确率；
- [ ] 输出错误检测召回率；
- [ ] 输出精确和宽松定位准确率；
- [ ] 输出误报率；
- [ ] 输出错误类型分布；
- [ ] 按难度分层；
- [ ] 按算法类型分层；
- [ ] 分析能力下降区间；
- [ ] 说明项目局限；
- [ ] 完成不超过 2 分钟的 demo。

### GitHub 交付安全

- [ ] README 首屏说明项目用途；
- [ ] README 提供 5 分钟运行方法；
- [ ] README 提供复现实验方法；
- [ ] README 标注个人/活动作品、非腾讯官方；
- [ ] 仓库没有 API Key；
- [ ] 仓库没有账户密码；
- [ ] 仓库没有私人敏感信息；
- [ ] `.env` 未进入 Git；
- [ ] CI 通过；
- [ ] 完成干净克隆复现；
- [ ] 最终提交链接可以公开访问。

---

## 18. 检查人员重点

希望检查人员重点确认：

1. 项目方向是否准确对应任务 2 的算法竞赛场景；
2. 是否明确区分普通 Online Judge 和过程评估；
3. 8 题 MVP 的规模是否合理；
4. E1—E8 错误分类是否足够清晰；
5. 四象限样本设计是否能够覆盖“答案正确但过程错误”；
6. 定位准确率与误报率定义是否符合任务要求；
7. 洛谷题目引用和独立测试策略是否合理；
8. 是否还缺少必须在开发前确定的硬约束；
9. 混合评估架构是否适合高中生独立完成；
10. 是否存在范围过大、难以按时交付的部分。

检查意见建议通过 GitHub Issues 提交，并标注：

```text
planning
```

或：

```text
review
```

---

## 19. 参考依据

### 原始任务书

《犀牛鸟开源-实战任务-混元大语言模型项目》，共 4 页：

- 通用提交要求：第 1 页；
- 任务 2 应用、题集、过程评估和验证要求：第 3—4 页；
- 核对日期：2026-09-06。

### Hy3

- Hy3 官方仓库：  
  https://github.com/Tencent-Hunyuan/Hy3

### 洛谷

- 洛谷题目难度体系：  
  https://help.luogu.com.cn/manual/luogu/problem/difficulty

- 洛谷题目与评测说明：  
  https://help.luogu.com.cn/manual/luogu/problem/

- 洛谷查找题目说明：  
  https://help.luogu.com.cn/manual/luogu/problem/search

---

## 20. 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-09-06 | 完成阶段 0 项目定位、需求边界、题集策略、评估架构、指标、路线图、风险和完成定义 |

---

## 项目声明

本项目是郭昊杨参与犀牛鸟开源实战任务的个人/活动作品，非腾讯官方发布。

项目将坚持可运行、可验证、可复现和不虚构结果的原则。所有未完成的功能、实验、人工复核和 demo 均会明确标注状态，不会被描述为已经完成。
